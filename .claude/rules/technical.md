# 기술 규칙

## 아키텍처
단일 책임 원칙(SRP)을 따르는 간단한 정적 유틸리티 클래스로 설계되었습니다. 모든 로직은 정적 메서드 내에 캡슐화되어 있으며, 상태를 유지하지 않습니다.

## 코드 스타일
PEP 8 스타일 가이드를 준수합니다. 클래스와 메서드에는 명확한 독스트링(Docstring)을 작성하고, 타입 힌트(typing)를 사용하여 가독성과 유지보수성을 높였습니다.

## 네이밍 컨벤션
클래스명은 PascalCase, 상수는 대문자 SNAKE_CASE, 메서드명은 snake_case를 사용합니다. 이름은 의도를 명확히 반영하도록 지었습니다(예: generate_incomplete_request_message).

## 사용 패턴
- Static Method Pattern: 인스턴스 생성 없이 유틸리티 기능을 제공
- Template Method Pattern (경향): 메시지의 구조(헤더, 본문, 푸터)를 고정된 형식으로 조립

## 의존성
Python 표준 라이브러리의 typing 모듈만 사용하므로 외부 의존성이 없습니다.

## 테스트
단위 테스트(예: unittest 또는 pytest)를 통해 generate_incomplete_request_message 메서드의 다양한 입력(user_input)에 대한 출력 메시지의 정확성과 포맷팅을 검증해야 합니다. SUGGESTIONS 리스트의 변경 사항도 테스트해야 합니다.

## 배포
독립 실행형 클래스이므로, 프로젝트의 유틸리티 모듈로 포함시키거나 패키징하여 PyPI에 배포할 수 있습니다. 별도의 서비스나 인프라가 필요 없습니다.

---
*Generated by octo*
